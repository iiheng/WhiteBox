package mirror.android.content

import android.content.pm.ProviderInfo
import android.os.IInterface
import mirror.RefBoolean
import mirror.RefClass
import mirror.RefObject

//object ContentProviderHolderOreo {
//    // 使用Kotlin的object关键字声明单例
//    val TYPE: Class<*> = RefClass.load(ContentProviderHolderOreo::class.java, "android.app.ContentProviderHolder")
//    // 使用委托属性的方式声明反射获取的对象
//    val info: RefObject<ProviderInfo> by lazy { RefObject() }
//    val provider: RefObject<IInterface> by lazy { RefObject() }
//    val noReleaseNeeded: RefBoolean by lazy { RefBoolean() }
//}
